lapsedUserAccounts.map( u => sendBillTo(u.address) );
function divide(a, b) {
  return a / b;
  test('divide divides 2000 by 100', () => {
    expect(divide(2000, 100)).toBe(20);
  });
}
let division = divide(2000, 100); 
const square = (parameter) => {
  return parameter * parameter;
};

const add = (parameter1, parameter2) => {
  return parameter1 + parameter2;
};

describe("Test function add",() =>{
  expect(add(parameter1+parameter2));
});
test('add arrow function takes two parameters and adds them together', () => {
  expect(add(10, 5)).toBe(15);
});



  
  
  


  
  
  

  
  
  